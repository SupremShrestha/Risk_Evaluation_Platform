# PostgreSQL / PostGIS
POSTGRES_USER=bipad_admin
POSTGRES_PASSWORD=changeme
POSTGRES_DB=bipad_risk

# Airflow container user ID — use the built-in image UID, not your host UID
# (see docs/known-issues.md for why)
AIRFLOW_UID=50000
